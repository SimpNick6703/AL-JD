[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  # Skip Python dependency installation
  NETLIFY_SKIP_PYTHON_INSTALL = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
